<!DOCTYPE html>
<html lang="{{group.locale}}">
  <head>
    <title>
      {{~#if (equals post.title group.name)~}}
        {{group.name}}
      {{~else~}}
        {{post.title}} · {{group.name}}
      {{~/if~}}
    </title>
    <meta name="description" content="Queer Tango Collective aims to enable personal and social transformation through the study, practice and enjoyment of the Argentine tango dance and music." />
    <meta name="keywords" content="Queer Tango Collective, Queer Tango Collectivo, QTC, Queer Tango, Queer Tango Club, Queer Tango NYC, Queer Tango New York, Tango, Tango Argentino, Tango New York, Tango in New York, Dancing, samesex, same sex tango, Gay Tango, Lesbian, Gay, Bisexual, Transgender" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <link rel="apple-touch-icon" href="/favicon.png">
    <link rel="shortcut icon" href="/favicon.png">
    {{#if post.description}}
      <meta name="description" content="{{post.description}}" />
      <meta property="og:description" content="{{post.description}}" />
    {{/if}}
    {{#if post.image}}
      <meta property="og:image" content="{{image-url post.image}}" />
    {{/if}}
    <meta property="og:site_name" content="{{group.name}}" />
    <meta property="og:title" content="{{post.title}}" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="{{post.url}}" />

    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="{{post.url}}">
    <meta property="twitter:title" content="{{post.title}}">
    <meta property="twitter:description" content="{{post.description}}">
    {{#if post.image}}
      <meta property="twitter:image" content="{{image-url post.image}}">
    {{/if}}

    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <main>
      <header>
        <h5>
          <a href="/" id="logo"><img src="https://builds.queertangocollective.org/admin/assets/images/black-logo-62ff338d6c4c313c8a096da504da0228.png"
              alt="{{group.name}}" title="{{group.name}}"></a>{{~_~}}
          <a href="/" id="text-logo">{{group.name}}</a>
        </h5>
        <nav>
          <ul>
            {{#each sections as |section|}}
              <li>
                {{#if (equals section.slug "home")}}
                  <a href="/" {{#if (equals section.id ../post.section.id)}} class="active" {{/if}}>
                    {{~section.name~}}
                  </a>
                {{else}}
                  <a href="/{{section.slug}}" {{#if (equals section.id ../post.section.id)}} class="active" {{/if}}>
                    {{~section.name~}}
                  </a>
                {{/if}}
              </li>
            {{/each}}
          </ul>
        </nav>
      </header>
      {{{yield}}}
    </main>
    <footer>
      <a href="mailto:{{group.email}}">{{ group.name }}</a>
    </footer>
    <script src="/scripts/gallery.js"></script>
    <script src="/scripts/header.js"></script>
  </body>
</html>